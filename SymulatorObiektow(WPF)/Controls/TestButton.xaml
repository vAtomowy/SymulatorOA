<UserControl x:Class="SymulatorObiektow_WPF_.Controls.TestButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SymulatorObiektow_WPF_.Controls"
             mc:Ignorable="d" 
             Height="49" Width="49">
    <ToggleButton BorderBrush="Transparent" Checked="ToggleButton_Checked" x:Name="TB">
        <ToggleButton.Background>
            <ImageBrush ImageSource="/SymulatorObiektow(WPF);component/g2994.png"/>
        </ToggleButton.Background>
        <ToggleButton.Style>
            <Style TargetType="{x:Type ToggleButton}">
                <Setter Property="BorderBrush" Value="{x:Null}"/>
                <Style.Triggers>
                    <Trigger Property="IsChecked" Value="true">
                        <Setter Property="BorderBrush" Value="Transparent"/>
                        <Setter Property="Background" Value="Transparent" />
                    </Trigger>
                </Style.Triggers>

            </Style>
        </ToggleButton.Style>

    </ToggleButton>
</UserControl>
